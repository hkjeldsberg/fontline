# FontLine uses Supabase for "public-first" persistence.
# The anon key is intentionally client-side: all tables have RLS disabled
# and the font-assets bucket is public. See README for the rationale.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
